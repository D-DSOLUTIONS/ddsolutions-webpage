# Protección para página de admin
<Files "admin-analytics.html">
    Order Deny,Allow
    Deny from all
    # Permitir solo IPs específicas (descomentar y agregar tu IP)
    # Allow from 123.456.789.0
</Files>

# Prevenir listado de directorios
Options -Indexes

# Bloquear acceso a archivos sensibles
<FilesMatch "\.(htaccess|htpasswd|ini|log|sh|sql|md)$">
    Order Allow,Deny
    Deny from all
</FilesMatch>